import { Component,inject } from '@angular/core';import { Router,RouterLink,RouterOutlet } from '@angular/router';import { AuthService } from './core/auth/auth.service';
@Component({selector:'app-root',standalone:true,imports:[RouterLink,RouterOutlet],template:`<header class="app-header"><a routerLink="/" class="brand">LearnFlow</a><nav><a routerLink="/dashboard">Dashboard</a><a routerLink="/learning-paths">Paths</a><a routerLink="/board">Board</a><a routerLink="/import">Import</a><a routerLink="/ai-planner">AI Planner</a><a routerLink="/notifications">Notifications</a>@if(auth.isAuthenticated()){<button type="button" (click)="logout()">Log out</button>}</nav></header><main class="shell"><router-outlet /></main>`})export class AppComponent{readonly auth=inject(AuthService);private router=inject(Router);logout(){this.auth.logout();void this.router.navigateByUrl('/login');}}
