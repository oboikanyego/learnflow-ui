import { Component, OnInit, inject, signal } from '@angular/core';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { MatButtonModule } from '@angular/material/button';
import { MatDialog, MatDialogModule } from '@angular/material/dialog';
import { ApiService } from '../../core/services/api.service';
import { ActionDialogComponent } from '../../shared/action-dialog.component';

interface GeneratedLesson { title:string; description?:string; date:string; time:string; durationMinutes:number; resourceUrl?:string; }
interface GeneratedModule { title:string; lessons:GeneratedLesson[]; }
interface GeneratedPhase { title:string; modules:GeneratedModule[]; }
interface GeneratedPlan { learningPath:{title:string;description?:string}; phases:GeneratedPhase[]; }
interface PlanJob { _id:string; status:'QUEUED'|'PROCESSING'|'COMPLETED'|'FAILED'; input:{topic:string;weeks:number;days:string[];time:string;durationMinutes:number;startDate:string;save:boolean}; plan?:GeneratedPlan; learningPathId?:string; errorMessage?:string; createdAt:string; completedAt?:string; }
interface QueueResponse { jobId:string; status:string; message:string; }

@Component({
  standalone:true,
  imports:[RouterLink,MatButtonModule,MatDialogModule],
  template:`
    <section class="page-enter ai-planner-page">
      <div class="page-head planner-head">
        <div><span class="eyebrow">AI-assisted planning</span><h1>AI learning planner</h1><p class="muted">Generate a structured roadmap without waiting on this page. LearnFlow will notify you when it is ready.</p></div>
        <div class="lesson-actions"><button mat-stroked-button (click)="openPlanner(false)">Generate preview</button><button mat-flat-button class="primary-cta" (click)="openPlanner(true)">Generate & save</button></div>
      </div>

      <section class="planner-config-card">
        <div><span class="mini-label">Current learning goal</span><h3>{{topic}}</h3></div>
        <div class="config-chips"><span>{{weeks}} weeks</span><span>{{days}}</span><span>{{time}}</span><span>Starts {{formatDate(startDate)}}</span></div>
      </section>

      @if (queueMessage()) {
        <section class="background-notice" aria-live="polite"><span class="background-icon">✦</span><div><strong>Generation started in the background</strong><p>{{queueMessage()}}</p></div><a routerLink="/dashboard">Continue using LearnFlow →</a></section>
      }
      @if(error()){<div class="notification unread"><strong>Planner error</strong><span>{{error()}}</span></div>}

      @if(plan();as generatedPlan){
        <section class="roadmap-shell">
          <div class="roadmap-hero"><div class="roadmap-copy"><span class="eyebrow">Generated learning roadmap</span><h2>{{generatedPlan.learningPath.title}}</h2><p>{{generatedPlan.learningPath.description||'A structured learning path generated around your preferred study schedule.'}}</p></div><div class="roadmap-stats"><article><strong>{{generatedPlan.phases.length}}</strong><span>Phases</span></article><article><strong>{{moduleCount(generatedPlan)}}</strong><span>Modules</span></article><article><strong>{{lessonCount(generatedPlan)}}</strong><span>Lessons</span></article><article><strong>{{totalHours(generatedPlan)}}</strong><span>Study hours</span></article></div></div>
          <div class="roadmap-timeline">
            @for(phase of generatedPlan.phases;track phase.title;let pi=$index){
              <section class="phase-block"><div class="phase-marker"><span>{{pi+1}}</span><div></div></div><div class="phase-content"><div class="phase-heading"><div><span class="phase-kicker">Phase {{pi+1}}</span><h3>{{phase.title}}</h3></div><span class="phase-meta">{{phase.modules.length}} modules · {{phaseLessonCount(phase)}} lessons</span></div><div class="module-grid">
                @for(module of phase.modules;track module.title;let mi=$index){<article class="generated-module"><div class="module-heading"><div><span class="module-number">{{pi+1}}.{{mi+1}}</span><h4>{{module.title}}</h4></div><span class="lesson-count">{{module.lessons.length}} lessons</span></div><div class="generated-lessons">
                  @for(lesson of module.lessons;track lesson.title;let li=$index){<div class="generated-lesson"><div class="lesson-index">{{li+1}}</div><div class="lesson-main"><div class="lesson-title-row"><strong>{{lesson.title}}</strong><span class="duration-badge">{{formatDuration(lesson.durationMinutes)}}</span></div>@if(lesson.description){<p>{{lesson.description}}</p>}<div class="lesson-schedule"><span>📅 {{formatDate(lesson.date)}}</span><span>🕒 {{formatTime(lesson.time)}}</span>@if(lesson.resourceUrl){<a [href]="lesson.resourceUrl" target="_blank" rel="noopener noreferrer">Open resource ↗</a>}</div></div></div>}
                </div></article>}
              </div></div></section>
            }
          </div>
          <div class="roadmap-footer"><div><strong>Your roadmap is ready.</strong><span>This preview remains available through your generation history and notification link.</span></div>@if(activeJob()?.learningPathId){<a mat-flat-button class="primary-cta" [routerLink]="['/learning-paths',activeJob()?.learningPathId]">Open saved path</a>}@else{<button mat-flat-button class="primary-cta" (click)="openPlanner(true)">Generate & save</button>}</div>
        </section>
      }

      <section class="generation-history">
        <div class="history-head"><div><span class="mini-label">Background activity</span><h3>Recent plan generations</h3></div><button mat-stroked-button (click)="loadJobs()">Refresh</button></div>
        @for(job of jobs();track job._id){<button type="button" class="job-row" (click)="openJob(job)"><span class="job-state" [class.done]="job.status==='COMPLETED'" [class.failed]="job.status==='FAILED'">{{job.status.replace('_',' ')}}</span><span class="job-copy"><strong>{{job.input.topic}}</strong><small>{{job.input.weeks}} weeks · {{job.input.days.join(', ')}} · {{formatDate(job.createdAt)}}</small></span><span class="job-action">{{job.status==='COMPLETED'?'Preview':'Details'}} →</span></button>}@empty{<div class="empty-jobs">No background generations yet.</div>}
      </section>
    </section>
  `,
  styles:[`
    .ai-planner-page{max-width:1280px;margin:0 auto}.planner-head{align-items:flex-end}.planner-config-card{display:flex;align-items:center;justify-content:space-between;gap:20px;padding:20px 22px;margin-bottom:24px;border:1px solid #dcdfe4;border-radius:12px;background:linear-gradient(135deg,#f8faff,#fff)}.planner-config-card h3{margin:4px 0 0;color:#172b4d}.config-chips{display:flex;gap:8px;flex-wrap:wrap}.config-chips span{padding:6px 10px;border-radius:999px;background:#e9f2ff;color:#0c4a9c;font-size:.75rem;font-weight:750}.background-notice{display:flex;align-items:center;gap:14px;padding:17px 18px;margin-bottom:24px;border:1px solid #b8d4ff;border-radius:12px;background:#f4f8ff}.background-icon{width:38px;height:38px;border-radius:10px;display:grid;place-items:center;background:linear-gradient(135deg,#0c66e4,#6554c0);color:#fff}.background-notice div{flex:1}.background-notice strong{color:#172b4d}.background-notice p{margin:2px 0 0;color:#626f86;font-size:.78rem}.background-notice a{color:#0c66e4;font-size:.78rem;font-weight:800;text-decoration:none}.roadmap-shell{margin-top:30px;border:1px solid #dcdfe4;border-radius:18px;background:#fff;overflow:hidden;box-shadow:0 16px 42px rgba(9,30,66,.08)}.roadmap-hero{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:34px;padding:32px;background:linear-gradient(135deg,#f7faff,#fff 58%,#f7f3ff);border-bottom:1px solid #dcdfe4}.roadmap-copy{max-width:700px}.roadmap-copy h2{margin:8px 0 10px;color:#172b4d}.roadmap-copy p{margin:0;color:#626f86}.roadmap-stats{display:grid;grid-template-columns:repeat(2,110px);gap:10px}.roadmap-stats article{padding:14px;border:1px solid #dfe5ee;border-radius:10px;background:#fff;display:flex;flex-direction:column}.roadmap-stats strong{font-size:1.45rem;color:#172b4d}.roadmap-stats span{font-size:.68rem;color:#626f86;font-weight:800;text-transform:uppercase}.roadmap-timeline{padding:28px 30px 10px}.phase-block{display:grid;grid-template-columns:42px minmax(0,1fr);gap:14px}.phase-marker{display:flex;flex-direction:column;align-items:center}.phase-marker>span{width:34px;height:34px;border-radius:10px;display:grid;place-items:center;background:linear-gradient(135deg,#0c66e4,#6554c0);color:#fff;font-weight:850}.phase-marker>div{width:2px;flex:1;background:#e2e8f0}.phase-content{padding:2px 0 30px}.phase-heading,.module-heading,.lesson-title-row,.roadmap-footer,.history-head{display:flex;justify-content:space-between;align-items:flex-start;gap:16px}.phase-kicker{color:#0c66e4;font-size:.68rem;font-weight:850;text-transform:uppercase}.phase-heading h3{margin:3px 0 14px;color:#172b4d}.phase-meta,.lesson-count{color:#7a869a;font-size:.7rem;font-weight:700}.module-grid{display:grid;gap:14px}.generated-module{border:1px solid #dcdfe4;border-radius:12px;background:#fafbfc;overflow:hidden}.module-heading{padding:14px 16px;background:#fff;border-bottom:1px solid #e5e7eb;align-items:center}.module-heading>div{display:flex;align-items:center;gap:10px}.module-heading h4{margin:0;color:#172b4d}.module-number{padding:4px 7px;border-radius:6px;background:#e9f2ff;color:#0c66e4;font-size:.68rem;font-weight:850}.generated-lessons{padding:4px 16px}.generated-lesson{display:grid;grid-template-columns:30px minmax(0,1fr);gap:11px;padding:14px 0;border-bottom:1px solid #e8eaed}.generated-lesson:last-child{border-bottom:0}.lesson-index{width:25px;height:25px;border-radius:7px;background:#f1f2f4;display:grid;place-items:center;font-size:.68rem;font-weight:800}.lesson-title-row strong{color:#172b4d}.duration-badge{padding:3px 7px;border-radius:999px;background:#e6f4ea;color:#216e4e;font-size:.65rem;font-weight:800}.lesson-main p{margin:5px 0 9px;color:#626f86;font-size:.76rem}.lesson-schedule{display:flex;gap:14px;flex-wrap:wrap;color:#626f86;font-size:.69rem}.lesson-schedule a{color:#0c66e4;font-weight:800;text-decoration:none}.roadmap-footer{align-items:center;padding:22px 30px;background:#f7f8f9;border-top:1px solid #dcdfe4}.roadmap-footer>div{display:flex;flex-direction:column}.roadmap-footer span{color:#626f86;font-size:.78rem}.generation-history{margin-top:28px;padding:22px;border:1px solid #dcdfe4;border-radius:14px}.history-head{align-items:center;margin-bottom:12px}.history-head h3{margin:3px 0 0}.job-row{width:100%;border:0;border-top:1px solid #e8eaed;background:#fff;padding:13px 4px;display:grid;grid-template-columns:110px minmax(0,1fr) auto;align-items:center;gap:12px;text-align:left;cursor:pointer}.job-row:hover{background:#fafbfc}.job-state{width:max-content;padding:4px 7px;border-radius:6px;background:#fff7d6;color:#7f5f01;font-size:.62rem;font-weight:850}.job-state.done{background:#e6f4ea;color:#216e4e}.job-state.failed{background:#ffebe6;color:#ae2a19}.job-copy{display:flex;flex-direction:column}.job-copy strong{color:#172b4d}.job-copy small{color:#7a869a}.job-action{color:#0c66e4;font-size:.72rem;font-weight:800}.empty-jobs{padding:20px 0;color:#7a869a}@media(max-width:850px){.planner-config-card,.roadmap-footer,.phase-heading{flex-direction:column}.roadmap-hero{grid-template-columns:1fr}.roadmap-stats{grid-template-columns:repeat(4,1fr)}.background-notice{align-items:flex-start;flex-wrap:wrap}.job-row{grid-template-columns:1fr auto}.job-state{grid-column:1/-1}}@media(max-width:580px){.roadmap-stats{grid-template-columns:repeat(2,1fr)}.roadmap-timeline{padding:22px 14px}.phase-block{grid-template-columns:30px minmax(0,1fr);gap:8px}.phase-marker>span{width:28px;height:28px}.generated-lessons{padding:2px 12px}.lesson-title-row{flex-direction:column}.job-action{display:none}}
  `]
})
export class AiPlannerComponent implements OnInit{
  private readonly api=inject(ApiService);private readonly dialog=inject(MatDialog);private readonly route=inject(ActivatedRoute);
  topic='React';weeks=6;startDate=new Date().toISOString().slice(0,10);time='19:00';days='Monday, Wednesday, Sunday';
  readonly error=signal('');readonly queueMessage=signal('');readonly plan=signal<GeneratedPlan|null>(null);readonly jobs=signal<PlanJob[]>([]);readonly activeJob=signal<PlanJob|null>(null);
  ngOnInit():void{this.loadJobs();const id=this.route.snapshot.queryParamMap.get('job');if(id)this.loadJob(id);}
  openPlanner(save:boolean):void{const ref=this.dialog.open(ActionDialogComponent,{width:'620px',data:{eyebrow:save?'Generate and save':'Generate preview',title:'Configure your learning plan',description:'Start generation and continue using LearnFlow while the AI builds your roadmap.',submitLabel:save?'Start generation & save':'Start generation',fields:[{key:'topic',label:'Topic or learning goal',type:'text',value:this.topic,required:true},{key:'weeks',label:'Number of weeks',type:'number',value:this.weeks,required:true,min:1},{key:'startDate',label:'Start date',type:'date',value:this.startDate,required:true},{key:'time',label:'Study time',type:'time',value:this.time,required:true},{key:'days',label:'Study days',type:'text',value:this.days,required:true,hint:'Example: Monday, Wednesday, Sunday'}]}});ref.afterClosed().subscribe(v=>{if(!v)return;this.topic=String(v.topic).trim();this.weeks=Number(v.weeks);this.startDate=String(v.startDate);this.time=String(v.time);this.days=String(v.days);this.queue(save);});}
  private queue(save:boolean):void{this.error.set('');this.queueMessage.set('');this.api.post<object,QueueResponse>('/api/v1/ai/generate-plan/background',{topic:this.topic,weeks:this.weeks,startDate:this.startDate,time:this.time,days:this.days.split(',').map(v=>v.trim()).filter(Boolean),durationMinutes:60,save}).subscribe({next:r=>{this.queueMessage.set(r.message);this.loadJobs();},error:e=>this.error.set(e.error?.message??'Could not start plan generation')});}
  loadJobs():void{this.api.get<PlanJob[]>('/api/v1/ai/plan-jobs').subscribe(v=>this.jobs.set(v));}
  loadJob(id:string):void{this.api.get<PlanJob>(`/api/v1/ai/plan-jobs/${id}`).subscribe({next:j=>this.openJob(j),error:e=>this.error.set(e.error?.message??'Could not load generated plan')});}
  openJob(job:PlanJob):void{this.activeJob.set(job);this.topic=job.input.topic;this.weeks=job.input.weeks;this.days=job.input.days.join(', ');this.time=job.input.time;this.startDate=job.input.startDate;if(job.status==='COMPLETED'&&job.plan){this.plan.set(job.plan);this.queueMessage.set('');}else if(job.status==='FAILED'){this.error.set(job.errorMessage??'This generation did not complete.');}else{this.queueMessage.set('This learning plan is still generating. You can continue using LearnFlow and return when the notification arrives.');}}
  moduleCount(p:GeneratedPlan){return p.phases.reduce((s,v)=>s+v.modules.length,0)}lessonCount(p:GeneratedPlan){return p.phases.reduce((s,v)=>s+this.phaseLessonCount(v),0)}phaseLessonCount(p:GeneratedPhase){return p.modules.reduce((s,v)=>s+v.lessons.length,0)}totalHours(p:GeneratedPlan){return (p.phases.flatMap(v=>v.modules).flatMap(v=>v.lessons).reduce((s,v)=>s+v.durationMinutes,0)/60).toFixed(1)}
  formatDuration(m:number){return m>=60?(m%60?`${Math.floor(m/60)}h ${m%60}m`:`${m/60}h`):`${m} min`}
  formatDate(v:string){const d=new Date(v.length===10?`${v}T12:00:00`:v);return Number.isNaN(d.getTime())?v:new Intl.DateTimeFormat(undefined,{day:'2-digit',month:'short',year:'numeric'}).format(d)}
  formatTime(v:string){const [h,m]=v.split(':').map(Number);if(Number.isNaN(h))return v;const d=new Date();d.setHours(h,m||0,0,0);return new Intl.DateTimeFormat(undefined,{hour:'2-digit',minute:'2-digit'}).format(d)}
}
