import { Component,inject,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { MatButtonModule } from '@angular/material/button';import { MatFormFieldModule } from '@angular/material/form-field';import { MatInputModule } from '@angular/material/input';import { ApiService } from '../../core/services/api.service';
@Component({standalone:true,imports:[FormsModule,MatButtonModule,MatFormFieldModule,MatInputModule],template:`<section><h1>AI learning planner</h1><p class="muted">Describe the skill and LearnFlow will produce a structured plan you can review.</p><div class="form-grid"><mat-form-field><mat-label>Topic</mat-label><input matInput [(ngModel)]="topic"></mat-form-field><mat-form-field><mat-label>Weeks</mat-label><input matInput type="number" [(ngModel)]="weeks"></mat-form-field><mat-form-field><mat-label>Start date</mat-label><input matInput type="date" [(ngModel)]="startDate"></mat-form-field><mat-form-field><mat-label>Study time</mat-label><input matInput type="time" [(ngModel)]="time"></mat-form-field><mat-form-field><mat-label>Days (comma separated)</mat-label><input matInput [(ngModel)]="days"></mat-form-field></div><button mat-flat-button (click)="generate()" [disabled]="busy()">{{busy()?'Generating…':'Generate plan'}}</button>@if(error()){<p>{{error()}}</p>}@if(plan()){<pre>{{json()}}</pre>}</section>`})
export class AiPlannerComponent{private api=inject(ApiService);topic='React';weeks=6;startDate=new Date().toISOString().slice(0,10);time='19:00';days='Monday, Wednesday, Sunday';busy=signal(false);error=signal('');plan=signal<unknown>(null);json(){return JSON.stringify(this.plan(),null,2);}generate(){this.busy.set(true);this.error.set('');this.api.post('/api/v1/ai/generate-plan',{topic:this.topic,weeks:this.weeks,startDate:this.startDate,time:this.time,days:this.days.split(',').map(x=>x.trim()).filter(Boolean),durationMinutes:60}).subscribe({next:p=>{this.plan.set(p);this.busy.set(false);},error:e=>{this.error.set(e.error?.message??'Could not generate a plan');this.busy.set(false);}});}}
