import { Component, OnInit, computed, inject, signal } from '@angular/core';
import { MatButtonModule } from '@angular/material/button';
import { RouterLink } from '@angular/router';
import { ApiService } from '../../core/services/api.service';

interface ReviewLesson {_id:string;title:string;description?:string;resourceUrl?:string;confidenceScore?:number;reviewStage?:number;nextReviewAt?:string;lastReviewedAt?:string;reviewCount?:number;masteryScore?:number;assessmentAttempts?:number;}
interface ReviewQueue { generatedAt:string; dueCount:number; weakCount:number; averageConfidence:number; due:ReviewLesson[]; upcoming:ReviewLesson[]; }
type ReviewTab='DUE'|'UPCOMING';

@Component({
  standalone:true,
  imports:[RouterLink,MatButtonModule],
  template:`
    <section class="retention page-enter">
      <div class="page-head"><div><span class="eyebrow">Adaptive retention</span><h1>Review what is starting to fade.</h1><p class="muted">LearnFlow schedules completed lessons back into your attention based on confidence, assessment evidence and previous review performance.</p></div><a mat-stroked-button routerLink="/mastery">View mastery</a></div>
      @if(queue();as data){
        <section class="summary-grid"><article><span>Due now</span><strong>{{data.dueCount}}</strong><small>Reviews ready for attention</small></article><article><span>Weak topics</span><strong>{{data.weakCount}}</strong><small>Confidence rated 1–2</small></article><article><span>Avg confidence</span><strong>{{data.averageConfidence || '—'}}<em>@if(data.averageConfidence){/5}</em></strong><small>Across scheduled reviews</small></article><article><span>Upcoming</span><strong>{{data.upcoming.length}}</strong><small>Reviews scheduled ahead</small></article></section>
        <section class="review-shell">
          <div class="section-head"><div><span class="mini-label">Review queue</span><h3>{{tab()==='DUE'?'Due for review':'Upcoming reviews'}}</h3></div><small>Work a small review set at a time instead of scrolling through the full queue.</small></div>
          <div class="tabs"><button type="button" [class.active]="tab()==='DUE'" (click)="selectTab('DUE')">Due <span>{{data.due.length}}</span></button><button type="button" [class.active]="tab()==='UPCOMING'" (click)="selectTab('UPCOMING')">Upcoming <span>{{data.upcoming.length}}</span></button></div>
          @if(tab()==='DUE'){
            <div class="review-list">
              @for(item of pagedDue();track item._id){
                <article class="review-card"><div class="review-copy"><span class="confidence" [attr.data-score]="item.confidenceScore??3">Confidence {{item.confidenceScore??3}} / 5</span>@if(item.masteryScore!==undefined){<span class="mastery-chip">Mastery {{item.masteryScore}}%</span>}<h3>{{item.title}}</h3><p>{{item.description || 'Revisit the important ideas, examples or commands from this lesson.'}}</p><div class="meta"><span>{{item.reviewCount??0}} previous reviews</span>@if(item.lastReviewedAt){<span>Last reviewed {{formatDate(item.lastReviewedAt)}}</span>}</div><div class="review-links">@if(item.resourceUrl){<a [href]="item.resourceUrl" target="_blank" rel="noopener">Open resource ↗</a>}<a [routerLink]="['/assess',item._id]">Take checkpoint →</a></div></div><div class="rating"><span>How well did you recall it?</span><div>@for(score of ratings;track score){<button type="button" (click)="review(item,score)" [disabled]="savingId()===item._id"><strong>{{score}}</strong><small>{{label(score)}}</small></button>}</div></div></article>
              } @empty {<div class="empty"><strong>You are caught up.</strong><span>No reviews are due right now.</span></div>}
            </div>
          } @else {
            <div class="upcoming-list">@for(item of pagedUpcoming();track item._id){<article><div><strong>{{item.title}}</strong><small>Confidence {{item.confidenceScore??3}}/5 · stage {{item.reviewStage??0}}@if(item.masteryScore!==undefined){ · mastery {{item.masteryScore}}%}</small></div><div class="upcoming-actions"><time>{{formatDate(item.nextReviewAt)}}</time><a [routerLink]="['/assess',item._id]">Checkpoint</a></div></article>}@empty{<div class="empty"><strong>No upcoming reviews.</strong><span>Your future review schedule will appear here.</span></div>}</div>
          }
          @if(pageCount()>1){<footer class="pager"><button mat-stroked-button type="button" (click)="setPage(page()-1)" [disabled]="page()===1">Previous</button><span>Page {{page()}} of {{pageCount()}}</span><button mat-stroked-button type="button" (click)="setPage(page()+1)" [disabled]="page()===pageCount()">Next</button></footer>}
        </section>
      } @else {<div class="loading">Building your review queue…</div>}
    </section>
  `,
  styles:[`
    .retention{max-width:1180px;margin:0 auto}.summary-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:18px}.summary-grid article{padding:18px;border:1px solid #dfe5ed;border-radius:16px;background:#fff}.summary-grid span{display:block;color:#7a869a;font-size:.68rem;font-weight:800;text-transform:uppercase}.summary-grid strong{display:block;color:#10233f;font-size:1.65rem;margin:5px 0}.summary-grid em{font-style:normal;font-size:.8rem;color:#7a869a}.summary-grid small{color:#66758a}.review-shell{padding:22px;border:1px solid #dfe5ed;border-radius:18px;background:#fff}.section-head{display:flex;justify-content:space-between;align-items:flex-end;gap:14px;margin-bottom:14px}.section-head h3{margin:4px 0 0;color:#10233f}.section-head>small{color:#7a869a;max-width:420px;text-align:right}.tabs{display:flex;gap:5px;padding:4px;background:#f2f4f7;border-radius:10px;width:max-content;max-width:100%;margin-bottom:14px}.tabs button{border:0;background:transparent;padding:8px 11px;border-radius:8px;color:#667085;font-weight:750;cursor:pointer}.tabs button.active{background:#fff;color:#175cd3;box-shadow:0 1px 3px rgba(16,24,40,.1)}.tabs span{display:inline!important;margin-left:5px;font-size:.62rem}.review-list{display:grid;gap:12px}.review-card{display:grid;grid-template-columns:minmax(0,1fr) 320px;gap:18px;padding:18px;border:1px solid #e2e8f0;border-radius:15px;background:#fbfcfe}.review-copy h3{margin:7px 0;color:#10233f}.review-copy p{color:#66758a;margin:0 0 10px}.confidence,.mastery-chip{display:inline-flex;padding:5px 8px;border-radius:999px;background:#eef4ff;color:#2f6fed;font-size:.65rem;font-weight:850;margin-right:5px}.confidence[data-score="1"],.confidence[data-score="2"]{background:#fff1f1;color:#b13b47}.mastery-chip{background:#eaf7ef;color:#216e4e}.meta{display:flex;gap:12px;flex-wrap:wrap;color:#7a869a;font-size:.68rem}.review-links{display:flex;gap:14px;flex-wrap:wrap}.review-copy a,.upcoming-actions a{display:inline-flex;margin-top:10px;color:#2f6fed;text-decoration:none;font-weight:800;font-size:.75rem}.rating{display:flex;flex-direction:column;justify-content:center;padding:13px;border-radius:12px;background:#fff;border:1px solid #e5e9ef}.rating>span{color:#526178;font-size:.72rem;font-weight:800;margin-bottom:8px}.rating>div{display:grid;grid-template-columns:repeat(5,1fr);gap:5px}.rating button{border:1px solid #dbe3ed;border-radius:9px;background:#fff;padding:7px 3px;cursor:pointer;color:#526178}.rating button strong,.rating button small{display:block}.rating button small{font-size:.52rem}.empty,.loading{padding:30px;border:1px dashed #cdd7e4;border-radius:14px;display:flex;flex-direction:column;gap:5px;color:#66758a}.empty strong{color:#10233f}.upcoming-list{display:grid}.upcoming-list article{display:flex;justify-content:space-between;gap:16px;padding:13px 0;border-top:1px solid #edf1f5}.upcoming-list article:first-child{border-top:0}.upcoming-list div{display:flex;flex-direction:column;min-width:0}.upcoming-list strong{color:#10233f;overflow-wrap:anywhere}.upcoming-list small,.upcoming-list time{color:#7a869a;font-size:.7rem}.upcoming-actions{align-items:flex-end}.upcoming-actions a{margin-top:3px}.pager{display:flex;justify-content:flex-end;align-items:center;gap:10px;padding-top:16px;margin-top:12px;border-top:1px solid #edf1f5}.pager span{color:#667085;font-size:.72rem}@media(max-width:900px){.summary-grid{grid-template-columns:1fr 1fr}.review-card{grid-template-columns:1fr}.section-head{align-items:flex-start;flex-direction:column}.section-head>small{text-align:left}}@media(max-width:560px){.summary-grid{grid-template-columns:1fr}.rating>div{grid-template-columns:repeat(3,1fr)}.upcoming-list article{flex-direction:column}.upcoming-actions{align-items:flex-start}.pager{justify-content:space-between}}
  `]
})
export class RetentionComponent implements OnInit{
  private readonly api=inject(ApiService);readonly queue=signal<ReviewQueue|null>(null);readonly savingId=signal('');readonly ratings=[1,2,3,4,5];readonly tab=signal<ReviewTab>('DUE');readonly page=signal(1);readonly pageSize=5;
  readonly pagedDue=computed(()=>{const start=(this.page()-1)*this.pageSize;return (this.queue()?.due??[]).slice(start,start+this.pageSize);});readonly pagedUpcoming=computed(()=>{const start=(this.page()-1)*this.pageSize;return (this.queue()?.upcoming??[]).slice(start,start+this.pageSize);});
  ngOnInit():void{this.load();}
  load():void{this.api.get<ReviewQueue>('/api/v1/retention/queue').subscribe(value=>{this.queue.set(value);this.setPage(1);});}
  selectTab(value:ReviewTab):void{this.tab.set(value);this.page.set(1);} pageCount():number{const length=this.tab()==='DUE'?(this.queue()?.due.length??0):(this.queue()?.upcoming.length??0);return Math.max(1,Math.ceil(length/this.pageSize));} setPage(value:number):void{this.page.set(Math.min(Math.max(1,value),this.pageCount()));}
  review(item:ReviewLesson,confidenceScore:number):void{this.savingId.set(item._id);this.api.post<{confidenceScore:number},ReviewLesson>(`/api/v1/retention/lessons/${item._id}/review`,{confidenceScore}).subscribe({next:()=>{this.savingId.set('');this.load();},error:()=>this.savingId.set('')});}
  label(score:number):string{return ['','Forgot','Hard','Okay','Good','Easy'][score];}
  formatDate(value?:string):string{return value?new Intl.DateTimeFormat(undefined,{day:'numeric',month:'short',year:'numeric'}).format(new Date(value)):'—';}
}
