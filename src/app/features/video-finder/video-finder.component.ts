import { Component, ElementRef, OnDestroy, OnInit, ViewChild, computed, inject, signal } from '@angular/core';
import { DomSanitizer, SafeResourceUrl } from '@angular/platform-browser';
import { FormsModule } from '@angular/forms';
import { ActivatedRoute } from '@angular/router';
import { MatButtonModule } from '@angular/material/button';
import { MatFormFieldModule } from '@angular/material/form-field';
import { MatInputModule } from '@angular/material/input';
import { ApiService } from '../../core/services/api.service';
import { Lesson } from '../../models/learning.models';

type DurationFilter = 'ALL' | 'SHORT' | 'MEDIUM' | 'LONG';
interface VideoLesson { id:string;title:string;description:string;status:string;scheduledAt?:string;durationMinutes:number;learningPathId?:string;resourceUrl?:string|null; }
interface LessonVideo { videoId:string;title:string;description:string;channelTitle:string;publishedAt:string;thumbnailUrl:string;watchUrl:string;embedUrl:string;durationIso:string;durationSeconds:number;durationLabel:string;hasCaptions:boolean;madeForKids:boolean;trackingDisabled:boolean;embeddable:boolean;privacyStatus:string; }
interface VideoSearchResponse { lesson:VideoLesson;requestedQuery:string;searchQuery:string;aiEnhanced:boolean;provider:string|null;videos:LessonVideo[]; }
interface PlayerHandle { destroy:()=>void;loadVideoById:(id:string)=>void;cueVideoById:(id:string)=>void; }
interface YoutubeApi { Player:new (element:HTMLIFrameElement|string,options:{events?:{onError?:(event:{data:number})=>void}})=>PlayerHandle; }
declare global { interface Window { YT?:YoutubeApi;onYouTubeIframeAPIReady?:()=>void; } }

@Component({
  standalone:true,
  imports:[FormsModule,MatButtonModule,MatFormFieldModule,MatInputModule],
  template:`
    <section class="video-page page-enter">
      <header class="page-head video-head"><div><span class="eyebrow">Learning library</span><h1>Find lesson videos</h1><p class="muted">Choose a lesson, let AI improve the search, then watch verified YouTube tutorials without leaving LearnFlow.</p></div><span class="youtube-badge">YouTube · AI assisted</span></header>
      <div class="finder-shell">
        <aside class="lesson-browser">
          <div class="panel-title"><div><span class="mini-label">Step 1</span><h2>Choose a lesson</h2></div><span>{{lessons().length}} shown</span></div>
          <div class="lesson-search-row"><mat-form-field appearance="outline"><mat-label>Search your lessons</mat-label><input matInput [(ngModel)]="lessonQuery" (keyup.enter)="searchLessons()" placeholder="e.g. RxJS operators"></mat-form-field><button mat-stroked-button type="button" (click)="searchLessons()" [disabled]="loadingLessons()">{{loadingLessons()?'Searching…':'Search'}}</button></div>
          <div class="lesson-list">@for(item of lessons();track item.id){<button type="button" class="lesson-option" [class.selected]="selectedLesson()?.id===item.id" (click)="selectLesson(item)"><span class="lesson-dot"></span><span class="lesson-option-copy"><strong>{{item.title}}</strong><small>{{statusLabel(item.status)}} · {{item.durationMinutes}} min</small></span><span class="lesson-arrow">›</span></button>} @empty {<div class="empty-small">{{loadingLessons()?'Loading lessons…':'No lessons matched your search.'}}</div>}</div>
        </aside>

        <main class="video-workspace">
          @if(selectedLesson();as lesson){
            <section class="selected-lesson-card"><div><span class="mini-label">Selected lesson</span><h2>{{lesson.title}}</h2><p>{{lesson.description || 'AI will use the lesson title to find practical learning material.'}}</p>@if(lesson.resourceUrl){<a [href]="lesson.resourceUrl" target="_blank" rel="noopener">Current lesson resource ↗</a>}</div><span class="duration-pill">{{lesson.durationMinutes}} min</span></section>
            <section class="discovery-card"><div class="discovery-copy"><span class="mini-label">Step 2</span><h3>What kind of video do you need?</h3><p>Add an optional refinement such as “beginner explanation”, “Angular example”, “interview prep” or “deep dive”.</p></div><div class="video-search-row"><mat-form-field appearance="outline"><mat-label>Refine the search</mat-label><input matInput [(ngModel)]="videoQuery" (keyup.enter)="findVideos()" placeholder="Optional refinement"></mat-form-field><button mat-flat-button class="find-button" type="button" (click)="findVideos()" [disabled]="searchingVideos()">{{searchingVideos()?'Finding videos…':'Find with AI'}}</button></div>@if(searchMeta();as meta){<div class="ai-query"><span>AI search</span><strong>{{meta.searchQuery}}</strong>@if(meta.aiEnhanced){<em>{{providerLabel(meta.provider)}}</em>}</div>}</section>
            @if(error()){<div class="error-card"><strong>Video search unavailable</strong><span>{{error()}}</span></div>}

            @if(selectedVideo();as video){
              <section class="player-card">
                <div class="player-frame">
                  @if(playerError()){
                    <div class="player-error-state"><strong>Could not play this video here</strong><span>{{playerError()}}</span><a [href]="video.watchUrl" target="_blank" rel="noopener">Watch on YouTube ↗</a></div>
                  } @else if(playerUrl()) {
                    <iframe #youtubeFrame id="learnflow-youtube-player" [src]="playerUrl()" [title]="video.title" (load)="connectPlayerEvents()" referrerpolicy="strict-origin-when-cross-origin" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe>
                  }
                </div>
                <div class="player-info"><div class="player-copy"><span class="mini-label">Now playing</span><h2>{{video.title}}</h2><p>{{video.channelTitle}} · {{video.durationLabel}} · {{publishedLabel(video.publishedAt)}}</p><div class="metadata-row">@if(video.hasCaptions){<span>CC captions</span>}@if(video.madeForKids){<span class="kids-badge">Made for Kids</span>}<span>Privacy-enhanced player</span></div>@if(video.madeForKids){<small class="kids-note">LearnFlow does not record player analytics for Made for Kids content.</small>}</div><div class="player-actions"><button mat-stroked-button type="button" (click)="saveSelectedVideo()" [disabled]="savingResource() || lesson.resourceUrl===video.watchUrl">{{lesson.resourceUrl===video.watchUrl?'Saved to lesson':savingResource()?'Saving…':'Save as lesson resource'}}</button><a [href]="video.watchUrl" target="_blank" rel="noopener">Open on YouTube ↗</a></div></div>
                @if(saveMessage()){<div class="save-message">{{saveMessage()}}</div>}
              </section>
            }

            @if(videos().length){
              <section class="results-section"><div class="results-head"><div><span class="mini-label">Step 3</span><h2>Recommended videos</h2></div><span>{{filteredVideos().length}} of {{videos().length}}</span></div>
                <div class="filter-row"><button type="button" [class.active]="durationFilter()==='ALL'" (click)="durationFilter.set('ALL')">Any length</button><button type="button" [class.active]="durationFilter()==='SHORT'" (click)="durationFilter.set('SHORT')">Under 10 min</button><button type="button" [class.active]="durationFilter()==='MEDIUM'" (click)="durationFilter.set('MEDIUM')">10–30 min</button><button type="button" [class.active]="durationFilter()==='LONG'" (click)="durationFilter.set('LONG')">30+ min</button></div>
                <div class="video-grid">@for(video of filteredVideos();track video.videoId){<button type="button" class="video-card" [class.active]="selectedVideo()?.videoId===video.videoId" (click)="play(video)"><span class="thumbnail-wrap"><img [src]="video.thumbnailUrl" [alt]="video.title" loading="lazy"><span class="play-mark">▶</span><span class="duration-overlay">{{video.durationLabel}}</span></span><span class="video-copy"><strong>{{video.title}}</strong><small>{{video.channelTitle}} · {{publishedLabel(video.publishedAt)}}</small><span>{{truncate(video.description,105)}}</span><span class="card-meta">@if(video.hasCaptions){<em>CC</em>}@if(video.madeForKids){<em>Made for Kids</em>}</span></span></button>} @empty {<div class="empty-filter">No videos match this duration filter.</div>}</div>
              </section>
            } @else if(!searchingVideos()) {<section class="video-empty"><div class="empty-icon">▶</div><h3>Ready to find a lesson video</h3><p>Use “Find with AI” to search YouTube. Results are verified for embedding and cached to protect API quota.</p></section>}
          } @else {<section class="video-empty choose-empty"><div class="empty-icon">⌕</div><h2>Select a lesson first</h2><p>Search your learning plan on the left, then choose the lesson you want to study with video.</p></section>}
        </main>
      </div>
    </section>
  `,
  styles:[`
    .video-page{max-width:1480px;margin:0 auto;overflow-x:hidden}.video-head{display:flex;justify-content:space-between;align-items:flex-start;gap:18px}.youtube-badge{flex:0 0 auto;padding:8px 11px;border-radius:999px;background:#fff1f0;color:#b42318;border:1px solid #fecdca;font-size:.7rem;font-weight:850}.finder-shell{display:grid;grid-template-columns:minmax(280px,340px) minmax(0,1fr);gap:18px;align-items:start}.lesson-browser,.selected-lesson-card,.discovery-card,.player-card,.results-section,.video-empty{background:#fff;border:1px solid #e4e7ec;border-radius:18px;box-shadow:0 1px 2px rgba(16,24,40,.03)}.lesson-browser{position:sticky;top:88px;padding:18px;max-height:calc(100vh - 110px);display:flex;flex-direction:column}.panel-title,.results-head,.selected-lesson-card,.player-info{display:flex;justify-content:space-between;align-items:flex-start;gap:14px}.panel-title h2,.results-head h2,.selected-lesson-card h2,.player-info h2{margin:4px 0;color:#101828}.panel-title>span,.results-head>span{font-size:.68rem;color:#667085}.lesson-search-row,.video-search-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px;align-items:start}.lesson-search-row{margin-top:12px}.lesson-search-row mat-form-field,.video-search-row mat-form-field{width:100%}.lesson-search-row button,.find-button{min-height:56px}.lesson-list{display:flex;flex-direction:column;gap:5px;overflow-y:auto;min-height:0}.lesson-option{width:100%;display:grid;grid-template-columns:10px minmax(0,1fr) auto;gap:10px;align-items:center;padding:11px;border:1px solid transparent;border-radius:12px;background:#fafbfc;text-align:left;cursor:pointer;color:#344054}.lesson-option:hover{background:#f2f4f7}.lesson-option.selected{background:#eef4ff;border-color:#b2ccff}.lesson-dot{width:7px;height:7px;border-radius:50%;background:#98a2b3}.lesson-option.selected .lesson-dot{background:#175cd3}.lesson-option-copy{min-width:0}.lesson-option-copy strong{display:block;font-size:.78rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.lesson-option-copy small{display:block;margin-top:4px;color:#667085;font-size:.64rem}.lesson-arrow{color:#98a2b3;font-size:1.2rem}.empty-small{padding:20px 8px;text-align:center;color:#667085;font-size:.75rem}.video-workspace{min-width:0;display:flex;flex-direction:column;gap:14px}.selected-lesson-card{padding:20px}.selected-lesson-card p{margin:5px 0;color:#667085;line-height:1.55;font-size:.78rem}.selected-lesson-card a,.player-actions a,.player-error-state a{color:#175cd3;text-decoration:none;font-size:.72rem;font-weight:800}.duration-pill{padding:6px 9px;border-radius:999px;background:#f2f4f7;color:#475467;font-size:.68rem;font-weight:800}.discovery-card{padding:20px}.discovery-copy h3{margin:4px 0;color:#101828}.discovery-copy p{margin:0 0 13px;color:#667085;font-size:.76rem;line-height:1.5}.find-button{background:#175cd3!important;color:#fff!important}.ai-query{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:4px;padding:10px 12px;border-radius:10px;background:#f8fafc;border:1px solid #eaecf0}.ai-query span{color:#667085;font-size:.65rem;font-weight:800;text-transform:uppercase}.ai-query strong{color:#344054;font-size:.74rem}.ai-query em{margin-left:auto;padding:3px 7px;border-radius:999px;background:#ecfdf3;color:#027a48;font-size:.62rem;font-style:normal;font-weight:800}.error-card{padding:13px 15px;border:1px solid #fecdca;background:#fef3f2;border-radius:12px;color:#b42318;display:flex;gap:7px;flex-direction:column;font-size:.76rem}.player-card{overflow:hidden}.player-frame{position:relative;width:100%;aspect-ratio:16/9;background:#101828;min-height:200px}.player-frame iframe{position:absolute;inset:0;width:100%;height:100%;border:0}.player-error-state{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;gap:8px;padding:24px;background:#101828;color:#fff}.player-error-state span{max-width:560px;color:#d0d5dd;font-size:.76rem}.player-error-state a{color:#84adff}.player-info{padding:17px 19px}.player-copy{min-width:0}.player-info h2{font-size:1.05rem}.player-info p{margin:4px 0;color:#667085;font-size:.73rem}.player-actions{display:flex;flex-direction:column;align-items:flex-end;gap:9px;flex:0 0 auto}.metadata-row,.card-meta{display:flex;gap:6px;flex-wrap:wrap}.metadata-row span,.card-meta em{padding:3px 6px;border-radius:999px;background:#f2f4f7;color:#475467;font-size:.6rem;font-style:normal;font-weight:800}.metadata-row .kids-badge,.card-meta em:last-child{background:#fff6ed;color:#b54708}.kids-note{display:block;margin-top:7px;color:#667085}.save-message{padding:9px 19px 14px;color:#027a48;font-size:.7rem;font-weight:800}.results-section{padding:18px}.filter-row{display:flex;gap:7px;flex-wrap:wrap;margin:13px 0}.filter-row button{border:1px solid #d0d5dd;background:#fff;border-radius:999px;padding:7px 10px;color:#475467;font-size:.68rem;font-weight:800;cursor:pointer}.filter-row button.active{border-color:#84adff;background:#eef4ff;color:#175cd3}.video-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.video-card{min-width:0;display:grid;grid-template-columns:145px minmax(0,1fr);gap:11px;padding:9px;border:1px solid #eaecf0;border-radius:13px;background:#fff;text-align:left;cursor:pointer}.video-card:hover,.video-card.active{border-color:#84adff;background:#f5f8ff}.thumbnail-wrap{position:relative;display:block;aspect-ratio:16/9;border-radius:9px;overflow:hidden;background:#101828}.thumbnail-wrap img{width:100%;height:100%;object-fit:cover;display:block}.play-mark{position:absolute;left:8px;bottom:7px;width:29px;height:29px;border-radius:50%;display:grid;place-items:center;background:rgba(16,24,40,.82);color:#fff;font-size:.65rem}.duration-overlay{position:absolute;right:6px;bottom:6px;padding:3px 5px;border-radius:5px;background:rgba(16,24,40,.84);color:#fff;font-size:.58rem;font-weight:800}.video-copy{min-width:0}.video-copy strong{display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;color:#101828;font-size:.78rem;line-height:1.35}.video-copy small{display:block;margin:6px 0;color:#475467;font-size:.65rem;font-weight:750}.video-copy>span:not(.card-meta){display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;color:#667085;font-size:.66rem;line-height:1.45}.card-meta{margin-top:6px}.empty-filter{grid-column:1/-1;padding:24px;text-align:center;color:#667085;font-size:.75rem}.video-empty{padding:48px 24px;text-align:center}.choose-empty{min-height:430px;display:flex;flex-direction:column;justify-content:center}.empty-icon{width:52px;height:52px;border-radius:15px;display:grid;place-items:center;margin:0 auto 13px;background:#eef4ff;color:#175cd3;font-weight:900}.video-empty h2,.video-empty h3{margin:0;color:#101828}.video-empty p{max-width:560px;margin:8px auto 0;color:#667085;line-height:1.6;font-size:.78rem}@media(max-width:1180px){.video-grid{grid-template-columns:1fr}.video-card{grid-template-columns:180px minmax(0,1fr)}}@media(max-width:900px){.finder-shell{grid-template-columns:1fr}.lesson-browser{position:static;max-height:none}.lesson-list{max-height:340px}.video-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.video-card{grid-template-columns:1fr}.player-frame{min-height:0}}@media(max-width:640px){.video-head,.selected-lesson-card,.player-info{flex-direction:column}.finder-shell{gap:12px}.lesson-browser,.selected-lesson-card,.discovery-card,.results-section{border-radius:14px}.lesson-search-row,.video-search-row{grid-template-columns:1fr}.lesson-search-row button,.find-button{width:100%;min-height:44px}.video-grid{grid-template-columns:1fr}.video-card{grid-template-columns:120px minmax(0,1fr)}.video-copy>span:not(.card-meta){display:none}.player-actions{width:100%;align-items:stretch}.player-actions button,.player-actions a{width:100%;text-align:center}.youtube-badge{align-self:flex-start}}@media(max-width:420px){.video-card{grid-template-columns:1fr}.video-copy>span:not(.card-meta){display:-webkit-box}.lesson-browser,.selected-lesson-card,.discovery-card,.results-section{padding:15px}.video-empty{padding:36px 18px}}
  `]
})
export class VideoFinderComponent implements OnInit,OnDestroy{
  private readonly api=inject(ApiService);private readonly route=inject(ActivatedRoute);private readonly sanitizer=inject(DomSanitizer);private player?:PlayerHandle;private youtubeReady?:Promise<void>;
  @ViewChild('youtubeFrame') youtubeFrame?:ElementRef<HTMLIFrameElement>;
  readonly lessons=signal<VideoLesson[]>([]);readonly selectedLesson=signal<VideoLesson|null>(null);readonly videos=signal<LessonVideo[]>([]);readonly selectedVideo=signal<LessonVideo|null>(null);readonly playerUrl=signal<SafeResourceUrl|null>(null);readonly searchMeta=signal<VideoSearchResponse|null>(null);readonly loadingLessons=signal(false);readonly searchingVideos=signal(false);readonly error=signal('');readonly playerError=signal('');readonly savingResource=signal(false);readonly saveMessage=signal('');readonly durationFilter=signal<DurationFilter>('ALL');
  readonly filteredVideos=computed(()=>this.videos().filter(video=>{const f=this.durationFilter();return f==='ALL'||(f==='SHORT'&&video.durationSeconds<600)||(f==='MEDIUM'&&video.durationSeconds>=600&&video.durationSeconds<1800)||(f==='LONG'&&video.durationSeconds>=1800);}));
  lessonQuery='';videoQuery='';
  ngOnInit():void{const lessonId=this.route.snapshot.queryParamMap.get('lessonId');this.searchLessons();if(lessonId){this.api.get<Lesson>(`/api/v1/lessons/${lessonId}`).subscribe({next:lesson=>this.selectLesson({id:lesson._id,title:lesson.title,description:lesson.description??'',status:lesson.status,scheduledAt:lesson.scheduledAt,durationMinutes:lesson.durationMinutes,resourceUrl:lesson.resourceUrl??null}),error:()=>undefined});}}
  ngOnDestroy():void{try{this.player?.destroy();}catch{}this.player=undefined;}
  searchLessons():void{this.loadingLessons.set(true);const query=this.lessonQuery.trim();this.api.get<VideoLesson[]>(`/api/v1/videos/lessons${query?`?q=${encodeURIComponent(query)}`:''}`).subscribe({next:items=>{this.lessons.set(items);this.loadingLessons.set(false);},error:()=>{this.lessons.set([]);this.loadingLessons.set(false);}});}
  selectLesson(lesson:VideoLesson):void{if(this.selectedLesson()?.id===lesson.id)return;this.selectedLesson.set(lesson);this.videos.set([]);this.clearPlayer();this.searchMeta.set(null);this.videoQuery='';this.error.set('');this.saveMessage.set('');this.durationFilter.set('ALL');}
  findVideos():void{const lesson=this.selectedLesson();if(!lesson||this.searchingVideos())return;this.searchingVideos.set(true);this.error.set('');this.saveMessage.set('');this.api.post<{lessonId:string;query?:string},VideoSearchResponse>('/api/v1/videos/search',{lessonId:lesson.id,...(this.videoQuery.trim()?{query:this.videoQuery.trim()}:{})}).subscribe({next:response=>{this.searchMeta.set(response);this.selectedLesson.update(current=>current?{...current,resourceUrl:response.lesson.resourceUrl??current.resourceUrl}:response.lesson);this.videos.set(response.videos);this.searchingVideos.set(false);this.durationFilter.set('ALL');const first=response.videos[0];if(first)this.setVideo(first,false);else this.clearPlayer();},error:event=>{this.videos.set([]);this.clearPlayer();const retry=event.error?.resetsAt?` Try again after ${new Date(event.error.resetsAt).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}.`:'';this.error.set((event.error?.message??'Could not find YouTube videos for this lesson.')+retry);this.searchingVideos.set(false);}});}
  play(video:LessonVideo):void{this.setVideo(video,true);}
  saveSelectedVideo():void{const lesson=this.selectedLesson(),video=this.selectedVideo();if(!lesson||!video||this.savingResource())return;this.savingResource.set(true);this.saveMessage.set('');this.api.post<{lessonId:string;videoId:string},{message:string;resourceUrl:string}>('/api/v1/videos/save-resource',{lessonId:lesson.id,videoId:video.videoId}).subscribe({next:result=>{this.selectedLesson.update(current=>current?{...current,resourceUrl:result.resourceUrl}:current);this.savingResource.set(false);this.saveMessage.set(result.message);},error:event=>{this.savingResource.set(false);this.saveMessage.set(event.error?.message??'Could not save this video to the lesson.');}});}
  private setVideo(video:LessonVideo,autoplay:boolean):void{this.selectedVideo.set(video);this.playerError.set('');this.saveMessage.set('');if(this.player){try{autoplay?this.player.loadVideoById(video.videoId):this.player.cueVideoById(video.videoId);return;}catch{this.player=undefined;}}this.playerUrl.set(this.buildPlayerUrl(video.videoId,autoplay));}
  private clearPlayer():void{try{this.player?.destroy();}catch{}this.player=undefined;this.selectedVideo.set(null);this.playerUrl.set(null);this.playerError.set('');}
  private buildPlayerUrl(videoId:string,autoplay:boolean):SafeResourceUrl{const origin=encodeURIComponent(window.location.origin);const url=`https://www.youtube-nocookie.com/embed/${encodeURIComponent(videoId)}?rel=0&enablejsapi=1&playsinline=1&origin=${origin}${autoplay?'&autoplay=1':''}`;return this.sanitizer.bypassSecurityTrustResourceUrl(url);}
  connectPlayerEvents():void{const frame=this.youtubeFrame?.nativeElement;if(!frame||this.player)return;void this.ensureYoutubeApi().then(()=>{if(!window.YT?.Player||!this.youtubeFrame||this.youtubeFrame.nativeElement!==frame||this.player)return;this.player=new window.YT.Player(frame,{events:{onError:event=>this.handlePlayerError(event.data)}});});}
  private ensureYoutubeApi():Promise<void>{if(window.YT?.Player)return Promise.resolve();if(this.youtubeReady)return this.youtubeReady;this.youtubeReady=new Promise(resolve=>{const previous=window.onYouTubeIframeAPIReady;window.onYouTubeIframeAPIReady=()=>{previous?.();resolve();};if(!document.querySelector('script[data-learnflow-youtube-api]')){const script=document.createElement('script');script.src='https://www.youtube.com/iframe_api';script.async=true;script.dataset['learnflowYoutubeApi']='true';document.head.appendChild(script);}});return this.youtubeReady;}
  private handlePlayerError(code:number):void{const messages:Record<number,string>={2:'YouTube rejected the video identifier.',5:'This video could not be played by the HTML5 player.',100:'This video was removed, made private, or is no longer available.',101:'The video owner does not allow this video to play in embedded players.',150:'The video owner does not allow this video to play in embedded players.',153:'YouTube could not verify the playback origin/referrer.'};this.player=undefined;this.playerError.set(messages[code]??'YouTube could not play this video in the embedded player.');}
  statusLabel(value:string):string{return value.replaceAll('_',' ').toLowerCase().replace(/(^|\s)\S/g,char=>char.toUpperCase());}
  providerLabel(provider:string|null):string{return provider?`${provider.charAt(0).toUpperCase()}${provider.slice(1)} assisted`:'AI assisted';}
  truncate(value:string,length:number):string{const text=value.trim();return text.length>length?`${text.slice(0,length).trim()}…`:text;}
  publishedLabel(value:string):string{if(!value)return'Publish date unavailable';const date=new Date(value);if(Number.isNaN(date.getTime()))return'Publish date unavailable';return new Intl.DateTimeFormat(undefined,{month:'short',year:'numeric'}).format(date);}
}
